[project]
name = "tickets"
version = "0.1.0"
description = "Lightweight lane-based ticket board with an MCP interface"
requires-python = ">=3.12"
dependencies = [
    "mcp>=2.2",
    "uvicorn>=0.30",
]

[dependency-groups]
dev = [
    "playwright>=1.63.0",
    "pytest>=9.1.1",
]
